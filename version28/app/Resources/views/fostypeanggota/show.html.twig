{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fostypeanggotum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fosTypeanggotum.id }}</td>
            </tr>
            <tr>
                <th>Namatype</th>
                <td>{{ fosTypeanggotum.namaType }}</td>
            </tr>
            <tr>
                <th>Deskripsitype</th>
                <td>{{ fosTypeanggotum.deskripsiType }}</td>
            </tr>
            <tr>
                <th>Statusdisplay</th>
                <td>{% if fosTypeanggotum.statusDisplay %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Createat</th>
                <td>{% if fosTypeanggotum.createAt %}{{ fosTypeanggotum.createAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Updateat</th>
                <td>{% if fosTypeanggotum.updateAt %}{{ fosTypeanggotum.updateAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fostypeanggota_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fostypeanggota_edit', { 'id': fosTypeanggotum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
